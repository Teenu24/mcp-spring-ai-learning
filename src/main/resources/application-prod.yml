spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    com.yourname.springmcp: WARN
    org.springframework.web: WARN

app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:}
